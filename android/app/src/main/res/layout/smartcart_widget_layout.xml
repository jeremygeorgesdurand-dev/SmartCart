<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="0dp">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="14dp"
        android:paddingEnd="10dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:background="@drawable/widget_header_background">

        <TextView
            android:id="@+id/widget_liste_nom"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="SmartCart"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:maxLines="1"
            android:ellipsize="end"/>

        <TextView
            android:id="@+id/widget_compteur"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="11sp"
            android:textColor="#CCFFFFFF"
            android:layout_marginEnd="10dp"/>

        <!-- Bascule le tri alphabétique / par rayon (voir SmartCartWidget.ACTION_TRI) -->
        <TextView
            android:id="@+id/widget_btn_tri"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:text="⇅"
            android:textSize="15sp"
            android:textColor="#FFFFFF"
            android:gravity="center"
            android:layout_marginEnd="6dp"
            android:background="@drawable/widget_btn_background"/>

        <TextView
            android:id="@+id/widget_btn_add"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:text="+"
            android:textSize="20sp"
            android:textColor="#FFFFFF"
            android:gravity="center"
            android:background="@drawable/widget_btn_background"/>
    </LinearLayout>

    <!-- Barre de progression -->
    <ProgressBar
        android:id="@+id/widget_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:max="100"
        android:progress="0"
        android:progressTint="#FFFFFF"
        android:progressBackgroundTint="#33FFFFFF"/>

    <!-- Articles : liste réellement défilable (RemoteViewsService), plus la
         limite fixe à 6 lignes d'avant qui masquait le reste sans recours. -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/widget_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:scrollbars="none"/>

        <!-- Message vide, affiché automatiquement par setEmptyView() quand
             l'adaptateur n'a aucune ligne. -->
        <TextView
            android:id="@+id/widget_empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="Configurez dans\nParamètres → Widget"
            android:textSize="11sp"
            android:textColor="#AAFFFFFF"
            android:gravity="center"/>

    </FrameLayout>

</LinearLayout>
